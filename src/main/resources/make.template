# Declaration of variables
CC = clang++
CC_FLAGS = -g -std=c++11
AR = ar
AR_FLAGS = -rv

# File names
EXEC = {{bin_name}}
SOURCES = $(wildcard *.cpp)
OBJECTS = $(SOURCES:.cpp=.o)

# Main target
$(EXEC): $(OBJECTS)
	$(CC) $(OBJECTS) -o $(EXEC)

library: $(OBJECTS)
	$(AR) $(AR_FLAGS) $(EXEC) $(OBJECTS)

# To obtain object files
%.o: %.cpp
	$(CC) -c $(CC_FLAGS) $< -o $@

# To remove generated files
clean:
	rm -f $(EXEC) $(OBJECTS)